<%= simple_form_for [@hotel, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: [0, 1, 2, 3, 4, 5], prompt: 'rate the hotel' %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
