<%= simple_form_for [@hotel, @room], :html => { :multipart => true }) do |f| %>
  <%= f.input :category %>
  <%= f.input :capacity %>
  <%= f.input :price %>
  <%= f.input :photos %>
  <%= f.input :photo, :multiple => true, name: "room_attachments[photo][]" %>
  <%= f.button :submit %>
<% end %>

